export default function CtaBanner() {
  return (
    <section className="cta-banner">
      <h2 className="cta-title">Ready to Create Something Unforgettable?</h2>
      <p className="cta-sub">Let's plan your next event together — from concept to standing ovation.</p>
      <a href="/get-quote" className="cta-big-btn" style={{ textDecoration: 'none' }}>Let's plan</a>
    </section>
  );
}
