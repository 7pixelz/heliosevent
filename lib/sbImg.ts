/**
 * Placeholder — Supabase image transform requires Pro plan.
 * Returns the original URL unchanged. Using Next.js <Image> for optimization instead.
 */
export function sbImg(url: string, _width: number, _quality = 80): string {
  return url ?? '';
}
